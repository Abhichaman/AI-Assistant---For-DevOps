[project]
name = "devops-shack-voiceops-assistant"
version = "1.0.0"
description = "DevOps Shack real-time voice assistant for local DevOps learning and read-only diagnostics."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "google-genai>=2.22",
    "python-dotenv>=1.0",
    "websockets>=13",
]

[tool.uv]
package = false
